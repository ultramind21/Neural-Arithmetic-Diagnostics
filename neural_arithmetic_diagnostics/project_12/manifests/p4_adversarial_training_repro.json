{
  "claim_ids": ["P4-C04"],
  "description": "Project 4 adversarial training re-run under Project 12 framework",
  "scope": "Validate narrow transfer pattern (seen family gain != held-out gain)",
  "intervention": {
    "name": "adversarial_training",
    "base_architecture": "mlp",
    "script_path": "project_12/scripts/interventions/project4_adversarial_training_repro.py",
    "base_checkpoint": "project_4/checkpoints/phase30_mlp_model.pt",
    "output_dir": "project_12/results/repro_p4/intervention/adversarial_training_v1"
  },
  "training_config": {
    "training_seed": 42,
    "num_epochs": 50,
    "batch_size": 32,
    "learning_rate": 0.001,
    "optimizer": "adam"
  },
  "family_config": {
    "seen_families": ["alternating_carry", "full_propagation_chain"],
    "heldout_family": "block_boundary_stress",
    "augmentation_ratio": 0.5
  },
  "evaluation_config": {
    "num_test_samples": 1000,
    "sequence_length": 8,
    "eval_seed": 223311
  },
  "validation_targets": {
    "P4-C04": {
      "metric": "narrow transfer (seen gain > held-out gain)",
      "threshold": {
        "seen_gain_minus_heldout_gain": {"min": 0.10}
      },
      "checks": [
        "improvement_on_seen_families_positive",
        "improvement_on_heldout_family_zero_or_negative",
        "transfer_ratio_less_than_0p25"
      ]
    }
  }
}
